// const data = require('../../fakedata/MOCK_DATA.json');

import data from '../../fakedata/MOCK_DATA.js';
// simport { db } from '../components/app';

// data.forEach((element) => {
//     console.log(element);
// });

// const text = JSON.parse(data());
console.log(JSON.parse(data()[2]));
